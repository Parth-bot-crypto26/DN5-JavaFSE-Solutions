# "Configure the application name in application.properties"
# lower-case here (see api-gateway's application.properties note on step 20)
spring.application.name=greet-service
server.port=8082

eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

logging.level.com.cognizant.greet=debug
